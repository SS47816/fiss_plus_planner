# defualt timeout, the actual value will be read from configuration file
timeout = 30 

# for timeout identification
# False by default since parallel processing will be the default choice
# if run_sequential_processing() is called, then it will be set to True
use_sequential_processing  = False 

